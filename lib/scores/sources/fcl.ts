import type { ScoreSource } from "./types";

/** Fútbol Costa Rica / FCL — plug fixtures or external feed later. */
export const fclSource: ScoreSource = {
  id: "fcl",
  name: "FCL",
  sport: "football",
  enabled: true,
};
