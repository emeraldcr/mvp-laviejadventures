import type { ScoreSource } from "./types";

export const premierLeagueSource: ScoreSource = {
  id: "premier-league",
  name: "Premier League",
  sport: "football",
  enabled: true,
  // Add seed() fixtures here when ready; admin can also create matches.
};
